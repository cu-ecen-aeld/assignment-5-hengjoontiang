config BR2_PACKAGE_MISC_MODULES
	bool "misc_modules"
	#need to enable Linux Kernel is checked under Kernel  in make menuconfig
	depends on BR2_LINUX_KERNEL
	help
	  The misc_modules package that contains scull and misc-modules
